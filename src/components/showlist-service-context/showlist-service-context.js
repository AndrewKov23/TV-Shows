import React from 'react';

const {
    Provider: ShowlistServiceProvider,
    Consumer: ShowlistServiceConsumer
} = React.createContext()

export {
    ShowlistServiceProvider,
    ShowlistServiceConsumer
}